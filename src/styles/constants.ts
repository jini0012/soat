export const focusRings = {
  default: "focus-visible:ring-2 focus-visible:ring-flesh-400 transition",
  container:
    "has-[:focus-visible]:ring-2 has-[:focus-visible]:ring-flesh-400 transition",
} as const;
